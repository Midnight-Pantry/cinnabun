{
  "name": "vite-ssr",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsc && node dist/server",
    "build:client": "vite build --outDir build/client",
    "build:server": "vite build --outDir build/server --ssr src/entry-server.js",
    "builda": "pnpm build:client && pnpm build:server"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "typescript": "^5.0.4",
    "vite": "^4.3.7"
  },
  "dependencies": {
    "cinnabun": "workspace:^",
    "express": "^4.18.2"
  }
}
